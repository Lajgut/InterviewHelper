# 🎤 Мок-интервью — трекер глубины ответов

> Формат: я задаю вопросы порциями (как интервьюер Яндекс/Ozon), ты отвечаешь.
> Я отмечаю глубину ответов. После всех тем — итоговый разбор + список на переповтор.
> Обновлено: 2026-08-31

**Шкала глубины:**
- 🔴 Нет ответа / совсем поверхностно — обязательно переповторять
- 🟡 Средне: ответил, но с пробелами / путаницей — выборочно переповторить
- 🟢 Глубоко: уверенно, с деталями и нюансами — ок

---

## Неделя 1 · Kotlin

| # | Тема | Глубина | Комментарий |
|---|------|--------|-------------|
| 1 | Null-safety, smart-casts | 🟡 | База верно + бонус: platform types (String!). НО: `!!` подан как основной способ (а он unsafe, главный — `?.`/`?:`/`let`); smart cast не раскрыт ВООБЩЕ (половина вопроса): не сказано про auto-приведение после if и 4 случая когда не работает (var-свойство, custom getter, open, var в лямбде). **Переповторить: smart cast cases + safe call/Elvis как основные инструменты** |
| 2 | let/run/apply/also/with | 🔴 | Критичная ошибка: сказал «let/run возвращают объект» — а они возвращают РЕЗУЛЬТАТ ЛЯМБДЫ (объект — только apply/also). Верно: it vs this ось, apply-пример, честно признал пробел. Не назвал: `?.let {}` (самый частый кейс), with — не extension. **Тема на зубрёжку — выучить таблицу + мнемонику «A = объект вернётся»** |
| 3 | inline/noinline/crossinline, reified | 🟡 | Верно: встраивание + нет аллокаций, non-local returns как мотивация, crossinline определён ТОЧНО, reified-связка с inline. Пробелы: не объяснил механику reified (type erasure → после встраивания тип известен в точке вызова), нет примеров (filterIsInstance, startActivity<T>), noinline не затронут, формулировка trade-off перевёрнута (при частых вызовах — code bloat). Дан подробный разбор с примерами (non-local vs return@label, postDelayed-задача). **Повторить: почему reified требует inline — одной фразой; noinline-кейс «сохранить лямбду»** |
| 4 | Дженерики, variance (in/out) | 🔴 | Термины кова/контра знает, связь с Java wildcards есть. НО ядро — соответствие in/out — НЕ знает («не помню, помоги запомнить»). Star-projection объяснил НЕВЕРНО — через «тип известен в рантайме и cast» (это type erasure, не star-projection); отличие List<*> от List<Any?> не знает. Примеры stdlib (List<out E>, Comparable<in T>) не назвал. Массивы Java (ArrayStoreException) — не знал. Дан разбор с мнемониками (out=output/producer, in=input/consumer, PECS). **Выучить: out/in мнемонику + List<*> vs List<Any?> + примеры stdlib** |
| 5 | Delegates (lazy, observable, custom) | 🟡 | Верно: lazy = создание при первом обращении + кеш (Lazy<T>); ключевое различие lazy/lateinit объяснил точно; вспомнил by remember/by viewModels. Ошибки: try-catch вместо `::prop.isInitialized` (антипаттерн); «lateinit — моветон» (нет: DI/тесты); эксепшен примерно (UninitializedPropertyAccessException). Не знал: LazyThreadSafetyMode (SYNCHRONIZED/PUBLICATION/NONE), ограничения lateinit (var, non-null, не примитивы), observable/vetoable, свой делегат = getValue/setValue операторы. **Повторить: режимы lazy, isInitialized, observable/vetoable** |
| 6 | Data/sealed classes, objects | 🟡 | Верно: генерируемые методы (3 из 5), механика бакетов hashCode, copy по сути, sealed vs enum зрело (даже компиляция), object. Пробелы: componentN не назван; контракт equals/hashCode не сформулирован словами («равные → равные hashCode, обратно нет»); ограничение sealed (наследники в том же модуле) не названо; copy с именованными аргументами + MVI/UDF кейс не назван; companion-механика не знал (реальный объект: интерфейсы, extensions, передача; @JvmStatic для Java). Дан разбор companion. **Повторить: полный список data-class методов, контракт equals/hashCode, sealed-ограничение, companion-фокус** |
| 7 | Коллекции vs Sequence | 🟡 | Верно: суть выигрыша (нет промежуточных аллокаций, «3 оператора = 3 коллекции»), практическое правило (1 оператор — смысла нет). ЯДРО не раскрыл: механика eager vs lazy («горизонтально» операция-за-операцией vs «вертикально» элемент-за-элементом), ранний выход (first() не трогает хвост), «без toList() не выполнится НИ ОДИН элемент». Терминология: перепутал «терминальный/промежуточный». sequence{}/generateSequence не знает. Дан разбор с картинкой-примером (6 вычислений+2 списка vs 2 вычисления+0). **Повторить: механику element-by-element + порог 1000+/ранний выход + builder** |
| 8 | ==/===, equals/hashCode | 🟡 | Верно: == → equals, === → ссылки; Integer cache -128..127 назвал ТОЧНО (сильный момент); суть строк Kotlin vs Java понял. Провал п.2: «почему == не падает при null» — не ответил механикой (компилируется в `a?.equals(b) ?: (b === null)`, null-ветка уходит в ===, equals не вызывается); интернирование строк не упомянул. **Повторить: формулу компиляции == (это же ответ на null-вопрос)** |
| 9 | Исключения, Result, runCatching | 🔴 | Верно: «в Kotlin все unchecked». Ошибки: п.3 ИНВЕРТИРОВАН — сказал «runCatching ловит всё КРОМЕ CancellationException», на деле ловит ВСЁ Throwable ВКЛЮЧАЯ CancellationException (главная опасность, ломает отмену корутин — kotlinx официально не рекомендует в suspend). @Throws не знает (только для Java-интеропа, в байткод throws). Операции Result не назвал. Код-ревью задачу не решил. Бонус-инсайт: «Result под запретом у нас в компании» — объяснены причины (нельзя как return type, глотание ошибок, CancellationException). **Повторить: runCatching = catch(Throwable) → CancellationException rethrow; ловить конкретные типы** |

## 🔁 Round 2 — Kotlin (пере-опрос после разбора ошибков)

> Начат 2026-09-07. Вопросы переформулированы, добавлены подвохи. Цель: закрыть 🔴 и подтянуть 🟡 до стабильных.
>
> **✅ ЗАВЕРШЁН 2026-09-07. Итог: 2🟢 · 7🟡 · 0🔴** (против R1: 0🟢 · 6🟡 · 3🔴).
> Все красные закрыты! Зелёные: scope functions, Sequence. Рабочая связка: «провал → эталон в разборе → повторение» — Sequence и scope functions доказали, что работает.
>
> **Системные остаточные дыры (пройдут через все темы, повторить вечером пн и вт):**
> 1. **hashCode-инерция** — механизм бакетов знает, К КОДУ не применяет (HashSet 2≠1, HashMap-кейс пропущен). Прогнать руками оба кейса.
> 2. **Smart cast var-свойство** — провален 2 раза. 4 случая списком + фикс «локальная val-копия».
> 3. **Формула ==** — «equals не вызовется при null» не звучит. Проговорить вслух 3 раза.
> 4. **componentN** — 2 раза мимо. Список 5 методов data class.
> 5. **«Рантайм знает тип»** — эразура не освоена: List<*> читается как Any?, никакой магии рантайма.

| # | Тема | Round 1 | Round 2 | Комментарий |
|---|------|---------|---------|-------------|
| 1 | Null-safety / smart cast | 🟡 | 🟡↓ | Код-задача: ②③ верно (компиляция определена правильно), но ① ГЛАВНЫЙ подвой провален повторно — «проблем нет» для var-свойства (не компилируется!). Тот же пробел, что в R1 — список 4 случаев отказа smart cast НЕ выучен. Фикс-идиома: локальная val-копия (`val n = name ?: return`). **СРОЧНО выучить 4 случая перед пятницей** |
| 2 | Scope functions | 🔴 | 🟢✅ | Код-задача: 4/4 верно (r1-r4, включая apply→объект и also→цепочка). Мелочь: println в r4 выведет "[1, 2, 3]", не "123". Часть 2: рабочий `name ?: return`, канонический — `name?.let { }`. ТЕМА ЗАКРЫТА, прогресс 🔴→🟢. Запрошено повторное объяснение with vs let vs run — дано (with = run, но аргументом) |
| 3 | inline/noinline/crossinline, reified | 🟡 | 🟡= | Ч.2: ИДЕЯ верная (reified + filterIsInstance по назначению), но синтаксис объявления неверный — «reified inline fun» вместо `inline fun <reified T>`; `<out T>` лишний. Ключевую строку `it is T` не написал. Ч.1: «не понимаю» — разобрано: вызов inline-параметра в non-inline контексте (Handler.post) → crossinline + сломанный контракт (нельзя вернуть T из async). **Повторить: форма `inline fun <reified T>`, кейс crossinline на Handler.post** |
| 4 | Variance | 🔴 | 🟡↑ | ОГРОМНЫЙ прогресс: п.1-4 закрыты — in/out освоены, катастрофа MutableList с Dog объяснена точно (главный вопрос темы!). п.5 провален ПОВТОРНО с той же ошибкой R1: «тип известен в рантайме» — на деле type erasure, читать из List<*> только как Any? (не Cat!); add запрещён не из-за immutable, а по сути star-projection (даже MutableList<*> не позволит). **Повторить: erasure-фразу + Any? при чтении из List&lt;*&gt;** |
| 5 | Делегаты | 🟡 | 🟡= | п.1 РЕГРЕССИЯ: «100 обращений = 100 вызовов connectToDb» — на деле SYNCHRONIZED = ОДИН вызов, остальные ждут кеш (lazy = кеширование!). PUBLICATION верно. п.2: эксепшен почти точно, проверка — синтаксис не тот (`::tracker.isInitialized`, не метод). п.3 верно по сути (на присваивание; не на объявление) + vetoable-добивка. Запрошены темы: свой делегат, Any/Object/Unit/Nothing/Throwable — добавлен блок в шпаргалку №11. **Повторить: lazy = 1 вызов; ::prop.isInitialized** |
| 6 | data/sealed, companion | 🟡 | 🟡↓ | п.1 ✅ (true), п.4 ✅ (ошибка компиляции — exhaustive when). п.2 ГЛАВНЫЙ ПРОВАЛ: сказал «2 элемента» — на деле 1 (hashCode совпадает → тот же бакет → equals → дубликат отвергнут). Обидно: в R1 механику бакетов объяснял правильно, но к коду не применил. п.3: componentN не знает ПОВТОРНО (шпаргалка блок 6 не дочитана). п.5: Companion.field — работает, но вопрос был про @JvmStatic. **Повторить: hashCode+equals → HashSet дубликаты; componentN; @JvmStatic** |
| 7 | Sequence | 🟡 | 🟢✅ | ИДЕАЛЬНО 4/4: результат 6 в обоих; A = 5 вычислений + 2 списка (посчитал сам); B = 3 вычисления, элементы 4-5 не тронуты («вертикальный проход» — механика усвоена!); без терминальной — ни одной операции. ТЕМА ЗАКРЫТА 🟡→🟢. Связка «провал→эталон→повторение» сработала — применить к hashCode-кейсу и smart cast |
| 8 | ==/===, equals | 🟡 | 🟡= | п.1: false ✅, но формула не звучит: не сказал «equals НЕ вызовется» (a?.equals(b) ?: (b===null)). п.2 ✅ true. п.3 ✅ вывод верен (1000 false / 100 true), формулировка неточна: «сравнение по значениям» — на деле == ВСЕГДА по ссылкам, кэш даёт ОДИН И ТОТ ЖЕ объект → ссылки совпадают. п.4 ПРОПУЩЕН: HashMap-кейс (разные hashCode → разные бакеты → get вернёт null, equals не вызовется; два «равных» = два ключа). hashCode-тема дыра ВТОРОЙ раз подряд! **Повторить: формулу ==, фразу про кэш-ссылки, HashMap-кейс** |
| 9 | Исключения, Result | 🔴 | 🟡↑ | п.1 ✅ cachedUser (проглотил отмену); п.2 ✅ СУТЬ верна — «корутина не закрылась, отмена перехвачена, надо rethrow» (инверсия R1 исправлена!); п.3 ❌ фикс-код не написал — эталон дан (try + CancellationException rethrow + конкретный catch); п.4 ⚠️ фраза частично в п.2. Каноничная фраза добавлена. Тема закрыта до 🟡↑ |

## Неделя 1–2 · Coroutines + Flow

| # | Тема | Глубина | Комментарий |
|---|------|--------|-------------|
| 1 | Structured concurrency, Scope/Job/SupervisorJob | 🟡↑ | Ч1 ✅ суть SC + выгоды (добавить формулу «отмена вниз, ошибка вверх»). Ч2.1 ✅✅ ОБА случая Job/SupervisorJob идеально — ядро темы закрыто. Ч2.2 ❌ lifetime: сказал «scope закроется после завершения корутин» — SupervisorJob живёт до явного cancel, scope B переиспользуемый; Job() в A отравлен (новые launch мгновенно отменяются). Ч3 ✅ viewModelScope + состав (SupervisorJob + Main.immediate). Не упомянул: без CoroutineExceptionHandler исключение в A крэшнет приложение. **Повторить: SupervisorJob умирает только от явного cancel; крэш-кейс** |
| 2 | suspend под капотом (CPS) | 🔴 | Полный провал: честные «не знаю» на все 4 части (CPS, continuation, state machine, инверсия стека). Тема глубокая, многие senior не знают, но Яндекс спрашивает. Дан эталон: CPS (скрытый параметр Continuation + возврат Any?/COROUTINE_SUSPENDED), state machine (switch(label) в continuation, локальные vars в heap-полях), физвызовы = числу реальных пауз+1, инверсия стека (стек → linked list continuation в heap). **Выучить эталон — будет козырем, отличающим от 90% кандидатов** |
| 3 | Dispatchers, withContext | 🟡 | Практика СИЛЬНАЯ: «withContext не даёт асинхронности» — ядро понял; async-решение верное (добивка: coroutineScope{}). Теория — дыра: пулы не знает (Default=CPU-ядра, IO=до 64, Main=1), Main.immediate механику не знает (инлайн если уже на main, без поста в очередь; viewModelScope=Main.immediate), Unconfined не знает, пустая лямбда → Unit не знает, Ч4 (Default/IO делят физический пул, слоты, дешёвый switch) не знает. Эталон дан + шпаргалка. **Повторить: размеры пулов + immediate-сценарий** |
| 4 | Cancellation (cooperative, NonCancellable) | 🟡↓ | Ч2 ✅ вывод кода ТОЧНЫЙ (work 0,1 → finally → done) — интуиция сильная. Но: «кооперативность» не раскрыта (нельзя убить принудительно, корутина уступает САМА: suspend-точки авто-проверяют, CPU-код — isActive/ensureActive); подвох suspend-в-finally не раскрыл; NonCancellable не знает (кейс: очистка/rollback при отмене); Ч4 угадал по сути без объяснения (while(true)+CPU = зависает навсегда). **Повторить: кооперативная = уступает сама; NonCancellable для finally-очистки** |
| 5 | Exceptions (Handler, launch vs async) | 🟡↓ | Ч3 ✅✅ coroutineScope/supervisorScope — полностью верно (+ добивка: rethrow первого). Ч1 полуверно с перепутыванием: async «передаёт скоупу» — на деле запечатано в Deferred до await(); «async не job» — неверно (Deferred IS Job); ловушка «await не вызван» = молчаливая потеря не знает. Ч2 ❌: сказал «крэшнет» — на деле «caught» напечатается: handler стоит в контексте КОРНЕВОГО launch → работает. Правило «handler только на корне launch» не знает. Эталон + шпаргалка 16. **Повторить: таблицу launch/async + правило handler-на-корне** |
| 6 | Channels, Select, Mutex | ⏭️ | Экспресс-режим (дедлайн): мок-вопросы пропущены, краткий эталон в шпаргалке №18 |
| 7 | Flow cold/hot, операторы | 🟡↑ | Частично закрыт в К6 (hot понял). Операторы/буферизация — шпаргалка №18 (экспресс без мока) |
| 8 | StateFlow vs SharedFlow vs LiveData | 🟡 | Ч1 база ✅ (default value, хранение последнего, hot) — replay/extraBufferCapacity не знает. Ч2①✅; ②❌ КЛАССИЧЕСКАЯ ОШИБКА: события через StateFlow — а сам же в ③ описал проблему повторной доставки (у StateFlow replay=1 = та же болезнь LiveData) → события = SharedFlow(replay=0); ③✅ sticky LiveData угадал. Ч3 hot ✅ (sticky-старт нового подписчика не уточнил). Ч4: вечный isLoading ✅, но «не упадём» ❌ — УПАДЁМ (SupervisorJob → uncaught → крэш), фикс try+rethrow Cancellation дан. **Повторить: события=SharedFlow(0); StateFlow крэш-кейс** |
| 9 | buffer/conflate/collectLatest | ⏭️ | Экспресс: эталон в шпаргалке №18 |
| 10 | callbackFlow, тестирование | ⏭️ | Экспресс: awaitClose в шпаргалке №18 |

> **Секция Coroutines завершена 10.09 (экспресс): 0🟢 · 5🟡 · 1🔴 + 3 темы шпаргалкой.**
> Итог: практическая интуиция сильная (код-задачи решает), теория под капот — дыры (suspend internals 🔴, dispatchers/handler-правила). Главное к пятнице: блоки шпаргалки 12–18 проговорить вслух.

## Неделя 3 · Compose (экспресс, 10.09)

| # | Тема | Глубина | Комментарий |
|---|------|--------|-------------|
| 1 | Recomposition + stability | 🟡 | Фиксы знает (@Immutable, ImmutableList, «лучше»), key-суть уловил («как diffUtil»), лямбда-проблему увидел. КЛЮЧЕВАЯ ОШИБКА: причина unstable — «поле mutable» — на деле ТИП List (интерфейс, нет гарантии иммутабельности), поля-то все val! Smart recomposition не знает (минимум функций: скип stable при неизменных параметрах; unstable → «на всякий случай» перезапуск). Фиксы лямбды: remember(key) + lambda memoization компилятора. Связь с weak-spot #1 (performance). **Повторить: unstable = ТИП, не val/var; skip-механика** |

## Неделя 4 · Android Core
_ожидает_

## Неделя 5 · Архитектура + System Design
_ожидает_

## Неделя 6 · Алгоритмы
_ожидает_

---

## 📊 Промежуточные итоги

### Секция Kotlin (завершена 2026-09-07): 6×🟡 · 3×🔴 · 0×🟢
**Вывод:** база есть по всем темам, но глубины 🟢 нет нигде. Секцию бы прошёл слабо-средне: интервьюер Яндекса докопается до механики.

**Сильный момент:** В8 — Integer cache -128..127 назван точно (редкость).
**Слабейшие темы:** scope functions (таблица не знает), variance (ядро in/out), Result/runCatching (инверсия + практика).

**Переповтор перед реальным интервью (по приоритету):**
1. 🔴 **Scope functions таблица** + мнемоника «A = объект вернётся» (apply/also)
2. 🔴 **Variance**: out=producer/output, in=consumer/input, PECS; List<*> vs List<Any?>; ArrayStoreException Java
3. 🔴 **runCatching/CancellationException** — критично, мост в секцию Coroutines!
4. 🟡 Smart cast: 4 случая когда не работает + `?.`/`?:` как главные инструменты (не `!!`)
5. 🟡 Sequence: element-by-element механика, ранний выход, порог 1000+
6. 🟡 Компаньон-механика (объект, интерфейсы, @JvmStatic); lazy-режимы; data class полный список (componentN); контракт equals/hashCode; sealed-ограничение (модуль)
| 2 | remember + side-effects | 🟡↓ | Ч1 ✅ (добивка: Saveable = Bundle → и смерть процесса). Ч2① ✅ баг «count застрянет» верно (фиксы: без remember / remember(ключ) / derivedStateOf). Ч2② ПРОТИВОРЕЧИЕ: «при каждой рекомпозиции» — неверно, LaunchedEffect ровно против этого: 1 раз на вход в композицию; НО при повороте — повтор (ловушка дублирования аналитики). Ч3: derivedStateOf не знает; SideEffect неверно (после КАЖДОЙ рекомпозиции, синк не-Compose мира); DisposableEffect половина (вход + onDispose). **Повторить: LaunchedEffect ≠ на рекомпозицию; три side-effect'а по строке** |

> **Compose-секция завершена (экспресс): 2×🟡. Ключ к завтра: блок шпаргалки №19–20.**

## Чт · Android Core (экспресс)

| # | Тема | Глубина | Комментарий |
|---|------|--------|-------------|
| 1 | MainThread/Looper + Lifecycle | 🟡↑ | Ч1 ✅ механика Looper/Queue/Handler + ANR=5с точно. Заблуждение: «postDelayed ждёт на другом потоке» — никто не ждёт, сообщение лежит в очереди до времени. Ч2: проблему уловил (мёртвые views), но lifecycleScope не вспомнил (авто-отмена / removeCallbacksAndMessages). Ч3 ✅✅ VM/поворот/SavedStateHandle/process death — зрелые ответы с кейсом (форма+СМС). Looper не на всех потоках (HandlerThread) — добивка |

> **Android Core завершён 1 вопросом (остальное — шпаргалка №21). ИТОГО МОК-КУРС: Kotlin R1 6🟡3🔴 → R2 2🟢7🟡; Coroutines 5🟡1🔴; Compose 2🟡; AC 1🟡↑.**
> Диагноз к пятнице: код-интуиция сильная; теория-механика — по шпаргалке. Все эталоны в cheatsheet.md (21 блок).

## 🌙 Вечерний блиц перед интервью (10.09)

| Раунд | Результат |
|-------|-----------|
| Блиц-1 (баги/coroutines/runBlocking) | 7/9: Б2 🟢 3/3 (исключение в Deferred без await — вчера путал!), Б3 ✅, Б1 скобку-мину пропустил (+hashCode-рассуждение верное) |
| Блиц-2 (suspend/retryIO/lifecycle) | Б4 полу✅ (CPS-скелет есть), Б5 🟡↑: cancellation-дисциплина освоена, НО вечный retry (счётчик не проверяется) + throw e вместо нового экземпляра; Б6 порядок ✅ идеален, очистка — onStop (onDestroy не гарантирован) |

**Динамика за день: явный рост. Код-интуиция + cancellation-дисциплина — на уровне.**

## 🏗️ Архитектура (экспресс, вечер 10.09 до 22:00)

| # | Тема | Глубина | Комментарий |
|---|------|--------|-------------|
| 1 | MVVM/MVI/MVP + UDF | 🟡 | MVI-суть ✅ (единый стейт), Ч4-кейс ✅ живой (переусложнённый MVI ВК). Ось MVP/MVVM (ссылка на View vs наблюдение) не названа; КРИТИЧЕСКАЯ ошибка: «LiveData в VM сбросятся при повороте» — VM переживает поворот (сам знал вчера!); проблема 3×LiveData = рассинхрон частичных состояний, не сброс. UDF-формула дана. **Повторить: VM переживает поворот; рассинхрон-проблема; ось ссылка-vs-наблюдение** |
| 2 | Clean Arch + многомодульность | 🟡↑ | Dependency Rule словесно ✅ (добавить формулу «зависимости внутрь»); DTO-в-репо нарушение ПОЙМАЛ (DataSource маппит, repo отдаёт Entity) + разумно про proxy-интеракторы; api/impl сильно + naming-плюс; кейсы ВК/Маруся живые. НЕ ЗНАЕТ by feature vs by layer (крупные = by feature, вертикальные срезы, параллельная сборка). **Повторить: by feature vs by layer + фразу Dependency Rule** |
| 3 | DI + SOLID (лайт) | 🟡 | DI-польза полная ✅ (IoC, граф, тестирование, подмены), God Object→SRP ✅. DIP-направление перепутал («низшие о высших» — наоборот); Hilt vs Koin аргумент не назвал (compile-time vs runtime-краш!); @Binds механика полуточно (живёт в @Module на абстрактном методе, не на Impl). SOLID пропущен по его самооценке — 5 строк в шпаргалке №22 |

> **Архитектура-экспресс завершена 21:40: 3×🟡. Все эталоны — шпаргалка №22. Чек-лист утра обновлён (архитектура — первой!).**

## ☀️ Утренний блиц перед интервью (11.09, 4 порции × 8)

- П1 (повторные провалы): 7/8 — В2 hashCode-коллизии объяснил идеально, smart cast/componentN/List<*>→Any? ЗАКРЫТЫ. 1 свап: runCatching↔runBlocking (исправлен)
- П2 (Coroutines): 5✅+3⚠️ (SupervisorJob-формула, CPS-сигнатура без suspend, «suspension points» вместо «стейт-машина»)
- П3 (Compose+AC): 8/8 — unstable=ТИП исправлен, lifecycle/ANR/VM идеальны
- П4 (Архитектура): 7/8 — рассинхрон-LiveData с примером, by feature, codegen-vs-runtime

**ВЕРДИКТ: готов. Все системные дыры закрыты. Идёт на интервью.**

## 💻 Этап 2 «Code mobile» — тренировка задач (с 13.09)

| Задача | Логика | Реализация | Комментарий |
|--------|--------|-----------|-------------|
| Частоты первых букв (CodeRun) | 🟡 | 🔴→🟢 | Идея HashMap частот сам; ревью-1: формат ввода (n строк, не через пробел), null+1, println(n) вместо ответа, мёртвый n/2-код; ревью-2 (исправил почти всё + ТРАССИРОВКА РУКАМИ — молодец): остался it vs commandString (lambda с pos → it не существует, unused var). Эталон выдан. **Уроки: формат ввода из условия; чек-лист перед сабмитом (5 пунктов) добавлен в план** |

## 📩 Фидбек этапа 1 Яндекса (получен 14.09)

**Итог: ПРОШЁЛ ✅.** Слабые места по самоотчёту:
1. Запутался в контрактах equals/hashCode
2. НЕ решил задачу: hashMap + объект без переопределённого hashCode

⚠️ Примечательно: это была дважды-проваленная тема моков, закрытая в утреннем блице — но на реальном интервью дала сбой. Вывод: для этой темы нужны ХИТРЫЕ ЗАДАЧИ руками, не теория.

**Действие:** создан weak-spots/03-equals-hashcode-puzzles.md — 10 хитрых задач с разборами (мутация ключа, симметричность, TreeMap-ловушка, константный hashCode и др.). Перед следующим интервью — прогнать ВСЕ руками.

## 📩 Этап 2 Яндекс (Code mobile) — ПРОВАЛЕН (сентябрь 2026)

Заявка активна, вероятен ретрай. Фидбек от кандидата — ожидается (обновить по получении).
Действия: продолжаем паттерны (DFS/бинарный/DP-1D/два указателя/жадность), периодически теория (weak-spots + equals/hashCode puzzles).
